# Production dependencies - FULLY PINNED to avoid dependency resolution issues

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic (data validation)
pydantic==2.5.3
pydantic-settings==2.1.0

# Database - Supabase client (using versions from local working environment)
supabase==2.24.0

# Other core dependencies
python-dotenv==1.0.0
loguru==0.7.2
PyYAML==6.0.1
typing-extensions==4.12.2
pydantic-core==2.14.6
annotated-types==0.7.0
starlette==0.35.1
click==8.3.1
pyjwt==2.10.1

# Uvicorn standard extras
watchfiles==1.1.1
websockets==11.0.3
uvloop==0.22.1
httptools==0.7.1

# Note: All versions explicitly pinned to prevent pip dependency resolver issues
