# Production dependencies - minimal set needed to run the API on Render

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic (data validation - comes with FastAPI but being explicit)
pydantic==2.5.3
pydantic-settings==2.1.0

# Database - Supabase client
supabase==2.3.4

# Environment variables
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# Note: All other dependencies (testing, code quality, Excel parsing, AI libraries)
# are development-only and not needed in production
