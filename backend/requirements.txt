# FastAPI and ASGI server
fastapi==0.128.8  # Security: CVE-2024-24762 + latest security patches
uvicorn[standard]==0.39.0  # Security: Latest stable with security fixes
python-multipart==0.0.27  # Security: CVE-2024-24762, CVE-2024-53981 + latest patches

# Database and ORM
supabase==2.28.3  # Security: Latest stable with auth/client fixes
psycopg2-binary==2.9.9
alembic==1.13.1
asyncpg==0.29.0

# Pydantic for data validation
pydantic==2.12.5  # Security: Latest patch with validation fixes
pydantic-settings==2.1.0

# Authentication and security
python-jose[cryptography]==3.5.0  # Security: CVE-2024-33663, CVE-2024-33664 fixed
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-dotenv==1.0.1
cryptography==46.0.6  # Security: CVE-2024-26130, CVE-2023-50782, CVE-2024-0727 + latest patches

# AI and ML
openai>=1.100.2  # Updated for standard-agent compatibility
anthropic==0.8.1
langchain==0.3.28  # Security: 12 CVEs fixed + latest patches (CVE-2024-28088, CVE-2024-8309, etc.)
langchain-openai==0.3.35  # Updated for langchain compatibility

# Jentic AI Agent Framework (integrated orchestration)
git+https://github.com/jentic/standard-agent.git@main
git+https://github.com/jentic/arazzo-engine.git@main#subdirectory=runner
litellm>=1.83.3  # Security: Latest stable with API security improvements
pyyaml>=6.0.1

# MCP (Model Context Protocol)
# Installed separately via: npx @modelcontextprotocol/server-playwright

# Redis for caching
redis==7.0.1  # Security: Latest stable with security improvements
hiredis==3.3.1  # Updated for redis 7.x compatibility

# Excel parsing
openpyxl==3.1.5  # Security: Latest stable with security patches
pandas==2.2.3  # Security: Updated for CVE fixes (conservative update to 2.2.x)
xlrd==2.0.1

# HTTP client (updated for jentic compatibility)
httpx>=0.28
websockets>=15.0.0
email-validator>=2.0.0
aiohttp==3.13.5  # Security: Latest stable with security patches

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code quality
black==26.3.1  # Security: CVE-2024-21503 (ReDoS) fixed
flake8==7.0.0
mypy==1.8.0
isort==5.13.2

# Logging and monitoring
loguru==0.7.2
sentry-sdk==2.57.0  # Security: CVE-2024-40647 (env variable exposure) + latest patches

# Utilities
python-dateutil==2.8.2
pytz==2023.3
